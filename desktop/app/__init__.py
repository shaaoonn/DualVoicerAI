"""Application package.

Houses the mixin classes and helper modules that ``main.py`` used to
inline. The split is purely structural — each mixin file is a focused
slice of what was previously the monolithic ``VoiceTypingApp`` class.

See ``app.app_core`` for the actual class assembly that ties the mixins
together.
"""
